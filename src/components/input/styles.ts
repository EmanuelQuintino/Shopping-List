import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    height: 48,
    width: "100%",
    paddingHorizontal: 16,
    backgroundColor: "#fff",
    borderColor: "#c3c5cb",
    borderWidth: 1,
    borderRadius: 8,
  },
});
