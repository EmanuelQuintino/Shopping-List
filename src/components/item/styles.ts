import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  item: {
    flexDirection: "row",
    alignItems: "center",
    gap: 8,
    marginVertical: 12,
  },

  description: {
    flex: 1,
    fontSize: 14,
    fontWeight: "600",
  },
});
