import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  filterButton: {
    flexDirection: "row",
    alignItems: "center",
    gap: 4,
  },
  title: {
    fontSize: 12,
    fontWeight: 600,
  },
});
